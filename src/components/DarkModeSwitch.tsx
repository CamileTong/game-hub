import { Switch } from "@/components/ui/switch";

const DarkModeSwitch = () => {
  return (
    <div>
      <Switch margin="3" colorPalette="green">
        Dark Mode
      </Switch>
    </div>
  );
};

export default DarkModeSwitch;
